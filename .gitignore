# Local backup - excluded from repo (uncomment to include if needed)
backend-laravel10-backup/

# OS files
.DS_Store
Thumbs.db

# Root env (nested projects already ignore their own .env via backend/.gitignore and frontend/.gitignore)
.env
.env.local

# Safety nets (nested .gitignores already cover these, but kept for root-level tooling)
**/node_modules/
**/vendor/
**/.next/
**/out/
**/coverage/
.phpunit.result.cache
